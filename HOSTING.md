# คู่มือขึ้นเว็บจริง (Hosting)

---

## สรุปสั้น ๆ ก่อน

**คำเตือนตอน `npm install` ไม่มีผลกับเว็บที่ขึ้นจริง**

สิ่งที่เห็นตอนติดตั้งเป็นเรื่องปกติของโปรเจกต์ Angular ทุกตัว:

| ข้อความ | ความหมาย | ต้องทำอะไรไหม |
|---|---|---|
| `npm warn deprecated tar / uuid / glob` | แพ็กเกจย่อยที่ Angular CLI เรียกใช้ภายในเป็นรุ่นเก่า | ไม่ต้อง |
| `33 vulnerabilities` | ช่องโหว่ในเครื่องมือ **ตอนพัฒนา** (Angular CLI, webpack, esbuild) | ไม่ต้อง |
| `182 packages are looking for funding` | แค่ขอรับบริจาคของผู้พัฒนาโอเพนซอร์ส | ไม่ต้อง |

**เหตุผล** — แพ็กเกจ 883 ตัวที่ติดตั้งเกือบทั้งหมดอยู่ใน `devDependencies`
คือเครื่องมือที่ใช้ **แปลงโค้ด** เท่านั้น ไม่ได้ถูกส่งขึ้นเว็บ

สิ่งที่ขึ้นเว็บจริงคือผลลัพธ์ของ `npm run build` ซึ่งเป็นไฟล์
HTML / CSS / JavaScript ที่แปลงเสร็จแล้ว ไม่มี `node_modules` ติดไปด้วยแม้แต่ไฟล์เดียว
เบราว์เซอร์ของกรรมการจึงไม่มีทางสัมผัสกับแพ็กเกจที่มีช่องโหว่เหล่านั้น

> ⚠️ **อย่ารัน `npm audit fix --force`**
> คำสั่งนั้นจะดาวน์เกรด Angular CLI ไปเวอร์ชันเก่ากว่ามาก แล้วโปรเจกต์จะ build ไม่ผ่านทันที
> ปล่อยไว้ตามเดิมถูกต้องแล้ว

---

## สิ่งที่ต้องระวังจริง ๆ มีข้อเดียว

### เส้นทางของ Single Page Application

เว็บนี้มี 6 หน้า (`/`, `/dimension-1` … `/dimension-5`) แต่บนเซิร์ฟเวอร์
มีไฟล์จริงแค่ `index.html` ไฟล์เดียว — Angular เป็นคนสลับหน้าให้ในเบราว์เซอร์

**ถ้าไม่ตั้งค่า** จะเกิดอาการนี้:

- กดเมนูจากหน้าแรก → ทำงานปกติ ✅
- แต่พอกรรมการ **กดรีเฟรช** ตอนอยู่หน้าด้านที่ 1 → ขึ้น **404 Not Found** ❌
- หรือ **แชร์ลิงก์** `เว็บ.com/dimension-3` ให้กรรมการเปิดตรง ๆ → ขึ้น 404 ❌

**วิธีแก้** คือบอกเซิร์ฟเวอร์ว่า "ถ้าหาไฟล์ไม่เจอ ให้ส่ง `index.html` กลับไป"
ซึ่งผมเตรียมไฟล์ตั้งค่าไว้ให้ครบแล้วทุกแบบ

---

## ขั้นตอนขึ้นเว็บ

### 1. สร้างไฟล์สำหรับขึ้นเว็บ

```bash
npm run build
```

### 2. อัปโหลดโฟลเดอร์นี้

```
dist/sansai-royal-award/browser/
```

> **สำคัญ** — ให้อัปโหลด **สิ่งที่อยู่ข้างใน** โฟลเดอร์ `browser`
> (ไฟล์ `index.html`, `main-xxxx.js`, `styles-xxxx.css` และไฟล์ตั้งค่า)
> ไม่ใช่อัปโหลดตัวโฟลเดอร์ `browser` ขึ้นไปทั้งอัน
>
> Angular รุ่นใหม่จะสร้างโฟลเดอร์ย่อยชื่อ `browser` ให้เสมอ คนมักพลาดตรงนี้

---

## ไฟล์ตั้งค่าตามชนิดของโฮสต์

ผมเตรียมไว้ให้แล้วทุกไฟล์ **ไม่ต้องสร้างเอง** ระบบจะคัดลอกไปให้อัตโนมัติตอน build

| โฮสต์ที่ใช้ | ไฟล์ที่ทำงาน | ต้องทำอะไรเพิ่ม |
|---|---|---|
| **cPanel / DirectAdmin / โฮสต์ไทยทั่วไป** (Apache) | `.htaccess` | ไม่ต้อง — อัปโหลดไปพร้อมกันก็ใช้ได้เลย |
| **Windows Server** (IIS) | `web.config` | ไม่ต้อง |
| **Netlify · Cloudflare Pages** | `_redirects` | ไม่ต้อง |
| **Vercel** | `vercel.json` (อยู่ที่โฟลเดอร์หลัก) | ไม่ต้อง |
| **GitHub Pages** | — | ต้องตั้งค่าเพิ่ม ดูหัวข้อถัดไป |

### ตรวจสอบว่าใช้ได้จริง

หลังอัปโหลดเสร็จ ให้ทดสอบ 2 อย่างนี้:

1. เปิดหน้าแรก → กดเมนู "ด้านที่ 3" → **กดรีเฟรช** ต้องยังอยู่หน้าเดิม ไม่ขึ้น 404
2. พิมพ์ `ชื่อเว็บ/dimension-5` ในช่อง URL ตรง ๆ ต้องเข้าได้

ถ้าข้อ 1 ขึ้น 404 แปลว่าไฟล์ `.htaccess` ไม่ได้ถูกอัปโหลดขึ้นไป
(โปรแกรม FTP บางตัวซ่อนไฟล์ที่ขึ้นต้นด้วยจุด — ต้องเปิดโหมด "แสดงไฟล์ซ่อน" ก่อน)

---

## กรณีขึ้นในโฟลเดอร์ย่อย

ถ้าเว็บไม่ได้อยู่ที่รากของโดเมน เช่นจะให้อยู่ที่
`www.sansai.ac.th/royal-award/` ต้องบอก Angular ด้วย:

```bash
npm run build -- --base-href=/royal-award/
```

แล้วแก้บรรทัด `RewriteBase` ใน `public/.htaccess` ให้ตรงกัน:

```apache
RewriteBase /royal-award/
```

ถ้าไม่ทำขั้นตอนนี้ หน้าเว็บจะขึ้นมาแต่ไฟล์ CSS/JS จะโหลดไม่เจอ (หน้าขาว)

---

## กรณีใช้ GitHub Pages

GitHub Pages ไม่รองรับการ rewrite เส้นทาง วิธีแก้ที่ง่ายที่สุดคือ
คัดลอก `index.html` เป็น `404.html` อีกไฟล์:

```bash
npm run build -- --base-href=/ชื่อ-repo/
cp dist/sansai-royal-award/browser/index.html dist/sansai-royal-award/browser/404.html
```

แล้วอัปโหลดทั้งโฟลเดอร์ขึ้น branch `gh-pages`

---

## ถ้าไม่อยากขึ้น host เลย

สำหรับวันประเมิน ไฟล์ **`preview.html`** ใช้งานได้ทันทีโดยไม่ต้องพึ่งอินเทอร์เน็ต
เลย — คัดลอกไฟล์เดียวลง iPad แล้วเปิดด้วย Safari ได้ทุกอย่างครบเหมือนกัน

วิธีที่แนะนำที่สุดสำหรับวันจริง:

1. ส่ง `preview.html` เข้า iPad (ผ่าน AirDrop, อีเมล หรือ Files)
2. เปิดด้วย Safari
3. กด **แชร์ → เพิ่มไปยังหน้าจอโฮม**
4. ได้ไอคอนเปิดแบบเต็มจอ ไม่มีแถบเบราว์เซอร์ และ **ไม่ต้องใช้เน็ต**

ข้อดีคือถ้าวันประเมิน Wi-Fi ของโรงเรียนมีปัญหา การนำเสนอก็ยังเดินหน้าต่อได้

> หมายเหตุเล็กน้อย — ฟอนต์ภาษาไทยดึงจาก Google Fonts ถ้าไม่มีเน็ต
> จะใช้ฟอนต์ของ iPad แทน หน้าตาต่างไปเล็กน้อยแต่อ่านได้ครบถ้วน

---

## ข้อควรรู้เพิ่มเติม

**ขนาดเว็บ** — ประมาณ 400–500 KB โหลดเร็วมากบน iPad แม้เน็ตช้า
เพราะไม่ได้ใช้ไลบรารีกราฟจากภายนอกเลย กราฟทั้งหมดวาดด้วย SVG ที่เขียนเอง

**ต้องต่ออินเทอร์เน็ตไหม** — ตัวเว็บไม่ต้องพึ่ง API ใด ๆ เป็นไฟล์นิ่งล้วน
ต่อเน็ตเฉพาะตอนโหลดฟอนต์ภาษาไทยจาก Google Fonts เท่านั้น

**อัปเดตข้อมูลภายหลัง** — แก้ไฟล์ใน `src/app/data/` แล้วรัน `npm run build`
อัปโหลดทับของเดิม ไฟล์ JS/CSS มีรหัสต่อท้ายชื่อใหม่ทุกครั้ง
กรรมการจึงเห็นข้อมูลใหม่ทันทีโดยไม่ต้องล้างแคช

**HTTPS** — ถ้าใช้โดเมนของโรงเรียน ควรเปิด SSL ด้วย (cPanel ส่วนใหญ่มี
Let's Encrypt ให้ฟรี) เพราะ Safari จะเตือนถ้าเว็บเป็น http ธรรมดา
